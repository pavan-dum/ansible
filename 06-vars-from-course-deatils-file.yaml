- name: "Vars accessing from course details yaml file"
  hosts: web
  become: yes
  vars-file:
    - course-details.yaml
  tasks:
    - name: "accessing course details"
      ansible.builtin.debug:
        msg: "course: {{ course }}, trainer: {{ trainer }}, duration: {{ duration }}"