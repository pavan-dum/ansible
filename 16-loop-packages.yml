- name: "Ansible Loop Packages"
  hosts: local
  connection: local
  become: yes

  tasks:
  - name: "Installing Packages"
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loops:
    - mysql
    - nginx
    - zip